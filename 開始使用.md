# 本機使用（解壓後）

電腦要先安裝 **Node.js 20 或以上**：https://nodejs.org （揀 LTS）。

## 代堂喺邊

側欄有「**代堂**」。瀏覽器亦可直接開：http://127.0.0.1:43217/cover

Copy 俾人時要 **成個資料夾** 一齊 copy（唔好只 copy 部份檔）。一定要有：

- `src/app/cover/page.tsx`（代堂頁）
- `src/app/api/cover/`（代堂 API）
- `src/lib/cover.ts`
- `src/components/app-shell.tsx`（側欄「代堂」）
- `fonts/DroidSansFallback.ttf`（匯出 PDF 用）

如果側欄冇「代堂」，即係 copy 唔齊。請用新嘅完整 zip 再解壓一次，唔好覆蓋漏檔。

## 最快開法

1. 解壓呢個資料夾。
2. 雙擊：
   - Windows：`本機啟動.bat`
   - macOS：`本機啟動.command`（第一次要右鍵 → 打開）
   - Linux：喺終端執行 `./本機啟動.sh`
3. 等終端出現 Ready 之後，瀏覽器打開：
   - http://127.0.0.1:43217
   - 代堂頁：http://127.0.0.1:43217/cover
   - 給同事複製網址：http://127.0.0.1:43217/share

要俾其他同事用：雙擊 `共用啟動.bat`，喺「給同事」頁複製校內網址。

第一次會自動 `npm install`，可能要一兩分鐘。

## 手動指令

```bash
cd 萬鈞伯裘書院課表-2026-2027
npm install
npm run dev
```

正式課表已喺 `data/schedule.json`，**唔使**再跑 `npm run seed`。如果之後要由 Excel 重建，先執行 `npm run seed`。

代堂結餘會寫入本機 `data/cover.json`，唔會影響課表檔。
